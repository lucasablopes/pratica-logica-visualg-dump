Algoritmo "EXERCICIOMENORDETRES"

Var

   x,y,z : real
   
Inicio

   escreva("Escreva três números: ")
   leia( x, y, z)
   escreval

   se (x < y) e (x < z) entao
      escreva("menor número = ", x)
      senao
           se y < z entao
           escreva("menor número = ", y)
           senao
           escreva("menor número = ", z)
      fimse
      fimse

Fimalgoritmo